<form style="width=50%" action="/topics/<%= post.topicId %>/posts/<%= post.id %>/comments/create" method="post">
  <div class="form-group">
    <label for="body">New Comment</label>
    <textarea name="body" class="form-control" aria-describedby="bodyHelp" placeholder="Enter comment"></textarea>
    <small class="text-muted" id="bodyHelp">Comment must be 5 or more characters in length.</small>
  </div>
  <button type="submit" class="btn btn-primary"></button>
</form>
